import dayjs from 'dayjs'
import { CBadge as Badge } from '@coreui/react'
import { IColumns } from '@/components/types'

export type T{{ columnTypeName }} = {
    {{ columnData }}
}

export const getColumns = (): IColumns<T{{ columnTypeName }}>[] => [
    {{ columns }}
]
